#include "monty.h"

/**
 * stack_mode_ep - Sets the stack mode.
 * @stack: Double pointer to the top of the stack.
 * @line_number: Line number in the source file where the function is called.
 */
void stack_mode_ep(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}

/**
 * queue_mode_ep - Sets the queue mode.
 * @stack: Double pointer to the top of the stack.
 * @line_number: Line number in the source file where the function is called.
 */
void queue_mode_ep(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
